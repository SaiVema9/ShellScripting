#!/bin/bash

# To read the values from user from command line in sequential order by using $1,$2,$3,$4 (These are called arguments) etc....... 
#The user must and should give in order only
#Then store the vlaue in the variable
#Note: There is no gap in equal to b/w variables & values

Name1=$1
Name2=$2

echo $Name1 :: is good person
echo  $Name2 :: is bad person for $Name1